using UnityEngine;
using System.Collections;

public class word_points_feed : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<Renderer>().sortingOrder = 15;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
